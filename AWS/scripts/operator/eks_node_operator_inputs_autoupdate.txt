# CrowdStrike Falcon Operator (EKS Node - AutoUpdate) Configuration
# This configuration is used with eks_node_operator_install_autoupdate.sh which
# pulls images directly from the CrowdStrike registry. AutoUpdate requires
# Falcon API credentials and unpinned images so the operator can resolve and
# update the Falcon sensor version automatically.

# Falcon API Credentials (obtain from CrowdStrike Falcon console)
# Required scopes:
#   - Falcon Images Download: Read
#   - Sensor Download: Read
FALCON_CLIENT_ID="your-falcon-client-id-here"
FALCON_CLIENT_SECRET="your-falcon-client-secret-here"

# AWS Configuration
AWS_REGION="your-aws-region"
AWS_PROFILE="default"

# EKS cluster name
CLUSTER_NAME="your-eks-cluster-name"

# Falcon Operator version to install (check https://github.com/crowdstrike/falcon-operator/releases for the latest release)
FALCON_OPERATOR_VERSION="v1.12.1"
